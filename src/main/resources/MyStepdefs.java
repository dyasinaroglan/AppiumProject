public class MyStepdefs {
    public MyStepdefs() {
        Given("^user on start page$", () -> {
        });
        When("^user sum the following numbers$", () -> {
        });
        Then("^the result should be (\\d+)$", (Integer arg0) -> {
        });
    }
}
